<!DOCTYPE html>
<html lang="en">
<head>
  <style>
      html {
          color-scheme: light dark;
      }

      body {
          margin: 2rem;
      }

      .logo-grid {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
          gap: 1.5rem;
          margin: 1.5rem 0;
      }

      .logo {
          border: 1px solid;
          padding: 1rem;
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: 0.75rem;
      }

      .logo-img {
          width: 100px;
          height: 100px;
          object-fit: contain;
      }

      .logo-name {
          margin: 0;
          text-align: center;
      }
  </style>
  <title>Hack Club Slack Icons!</title>
</head>
<body>
<h1>Hack Club Slack Icons!</h1>

<h3>Current:</h3>
<%= erb :logo, {}, logo: @current_logo %>

<h3>Possible icons: </h3>
<div class="logo-grid">
  <% @all_community_logos[1..].reverse.each do |logo| %>
  <%= erb :logo, {}, logo: %>
  <% end %>
</div>

<b>Possible YSWS icons: </b>
<div class="logo-grid">
  <% @all_ysws_logos[1..].each do |logo| %>
    <%= erb :logo, {}, logo: %>
  <% end %>
</div>
</body>
</html>
