<!DOCTYPE html>
<html lang="en">
<head>
  <style>
      html {
          color-scheme: light dark;
      }

      body {
          margin: 2rem;
      }

      .logo-grid {
          display: grid;
          grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
          gap: 1.5rem;
          margin: 1.5rem 0;
      }

      .logo {
          border: 1px solid;
          padding: 1rem;
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: 0.75rem;
      }

      .logo.current {
          background: black;
          color: white;
      }

      @media (prefers-color-scheme: dark) {
          .logo.current {
              background: white;
              color: black;
          }
      }


      .logo-img {
          width: 100px;
          height: 100px;
          object-fit: contain;
      }

      .logo-name {
          margin: 0;
          text-align: center;
      }
  </style>
  <title>Hack Club Slack Icons!</title>
</head>
<body>
<h1>Hack Club Slack Icons!</h1>

<h3>Current:</h3>
<%= erb :logo, {}, logo: @current_logo %>

<h3>Possible icons: </h3>
<i>(if you work here, use <a href="https://airtable.com/appOOH0x03NEHw8Rt/tbl1blRyjt0AUVBvJ/" target="_blank">this base</a> to add new icons to the pool!)</i>
<%= erb :logo_set, {}, logos: @all_community_logos %>


<h3>Possible YSWS icons: </h3>
<i>(if you're a program author, make sure your Unified DB "YSWS Programs" record has something in the <code>Slack Icon</code> field!)</i>
<%= erb :logo_set, {}, logos: @all_ysws_logos %>
</body>
</html>
